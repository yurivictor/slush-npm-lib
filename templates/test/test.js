/*!
 * <%= appName %> — tests
 */

'use strict';

/**
 * Module dependencies
 * @private
 */
const chai = require( 'chai' );
const expect = chai.expect;
const <%= appName %> = require( '../index.js' );

const client = <%= appName %>();
